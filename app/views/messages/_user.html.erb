<% user ||= local_assigns[:message] %>
<div id="<%= dom_id(user) %>" class="message message--user">
  <div class="message__bubble">
    <div class="message__text" id="<%= dom_id(user, :content) %>"><%= simple_format(user.content) %></div>
  </div>
</div>
